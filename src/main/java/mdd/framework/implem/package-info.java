/**
 * The `implem` package -- as the indicated by the name, provides the 
 * implementation of classes which you will want to use in order to 
 * instanciate the abstractions described in the other two packages.
 * 
 * The `implem` package is respectively split in several subpackages
 * each of which provides one or more implementations for the same
 * abstraction.
 */
package mdd.framework.implem;
